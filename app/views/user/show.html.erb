	<div class="card text-center mt-3">
	  <h1 class="card-header">Espace de <%= @user.first_name %> <%= @user.last_name %></h1>
	  <div class="card-body"> 
	    <h5 class="card-title">Description : <span class="font-italic"><%= @user.description %></span></h5>
	    <p class="card-text"><%= @user.first_name %> a <%= @user.age %> ans et habite <%= link_to @user.city.name, city_path(@user.city.id), class: "text-primary"  %>. Contactez cet auteur à l'adresse email suivante : <%= @user.email %></p>
	  </div>
	</div>
	<div class="row mt-3">
	  <% @user.gossips.each do |gossip| %>
		<div class="card mx-auto mb-5" style="width: 30rem;">
		  <div class="card-body">
		  <h5 class="card-title"><%= gossip.title %></h5>
		  <p class="card-text"><%= gossip.content %></p>
		  <%= link_to "Voir le potin", gossip_path(gossip.id), :class => "btn btn-secondary"%>
		  </div>
		</div>
	  <% end %>
	</div>